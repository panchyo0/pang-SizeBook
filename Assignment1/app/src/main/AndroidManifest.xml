<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.panchy.assignment1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ListOfRecord"
            android:label="@string/title_activity_list_of_record"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AddToData"
            android:label="@string/title_activity_add_to_data"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Detail" />
        <activity android:name=".Detail_edit"></activity>
    </application>

</manifest>